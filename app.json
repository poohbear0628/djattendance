{
  "name": "djattendance",
  "repository": "https://github.com/attendanceproject/djattendance",
  "success_url": "/",
  "scripts": {
    "postdeploy": "python ap/makeallmigrations.py && python ap/manage.py migrate --noinput && webpack --config webpack/webpack.prod.config.js --progress --colors && python ap/manage.py collectstatic"
  },
  "stack": "heroku-16",
  "buildpacks": [
    {
      "url": "heroku/python"
    },
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "DJANGO_SECRET_KEY": {
      "description": "A randomly generated secret to secure your Django installation",
      "generator": "secret"
    },
    "DJANGO_SETTINGS_MODULE": "ap.settings.staging"
  },
  "addons": [
    {
      "plan": "heroku-postgresql",
      "options": {
        "version": "9.5"
      }
    }
  ]
}
