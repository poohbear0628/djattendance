{
  "name": "djattendance",
  "repository": "https://github.com/attendanceproject/djattendance",
  "success_url": "/",
  "scripts": {
    "postdeploy": "python ap/makeallmigrations.py && python ap/manage.py migrate --noinput && python ap/manage.py collectstatic --noinput && python ap/manage.py populate && echo \"from accounts.models import User; User.objects.filter(email='admin@example.com').delete(); User.objects.create_superuser('admin', 'admin@example.com', 'admin')\" | python ap/manage.py shell"
  },
  "stack": "heroku-16",
  "buildpacks": [
    {
      "url": "heroku/python"
    },
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "DJANGO_SECRET_KEY": {
      "description": "A randomly generated secret to secure your Django installation",
      "generator": "secret"
    },
    "DJANGO_SETTINGS_MODULE": "ap.settings.staging"
  },
  "addons": [
    {
      "plan": "heroku-postgresql",
      "options": {
        "version": "9.5"
      }
    }
  ]
}
